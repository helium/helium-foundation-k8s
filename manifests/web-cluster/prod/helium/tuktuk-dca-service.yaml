apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: tuktuk-dca-service
  name: tuktuk-dca-service
  namespace: helium
spec:
  selector:
    matchLabels:
      app: tuktuk-dca-service
  template:
    metadata:
      labels:
        app: tuktuk-dca-service
        security-group: public-rds-access
    spec:
      serviceAccountName: public-monitoring-rds-monitoring-user-access
      containers:
        - name: tuktuk-dca-service
          image: public.ecr.aws/v0j6k5v6/tuktuk-dca-service:0.0.3
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8123
              name: http
              protocol: TCP
          env:
            - name: SOLANA_URL
              valueFrom:
                secretKeyRef:
                  name: helius-rpc-url
                  key: secret
            - name: PGHOST
              value: monitoring-rds.cbhihwhsofyu.us-west-2.rds.amazonaws.com
            - name: PGPORT
              value: "5432"
            - name: PGUSER
              value: monitoring
            - name: AWS_REGION
              value: us-west-2
            - name: PGDATABASE
              value: monitoring
            - name: PGSSLMODE
              value: no-verify
            - name: ANCHOR_WALLET
              value: /usr/src/app/keys/keypair.json
            - name: ORIGIN
              value: https://tuktuk-dca.web.helium.io

          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /health
              port: 8123
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 2
          livenessProbe:
            failureThreshold: 3
            initialDelaySeconds: 30
            periodSeconds: 10
            successThreshold: 1
            tcpSocket:
              port: 8123
            timeoutSeconds: 1
          volumeMounts:
            - name: tuktuk-dca-keypair
              mountPath: /usr/src/app/keys
              readOnly: true
          resources:
            requests:
              cpu: 200m
              memory: 300Mi
            limits:
              cpu: 400m
              memory: 400Mi
      volumes:
        - name: tuktuk-dca-keypair
          secret:
            secretName: tuktuk-dca-keypair
            items:
              - key: keypair.json
                path: keypair.json
---
apiVersion: v1
kind: Service
metadata:
  name: tuktuk-dca-service
  namespace: helium
spec:
  ports:
    - port: 8123
      protocol: TCP
      targetPort: http
  selector:
    app: tuktuk-dca-service
  sessionAffinity: None
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tuktuk-dca-service
  namespace: helium
  annotations:
    nginx.ingress.kubernetes.io/limit-rps: "10"
    nginx.ingress.kubernetes.io/limit-burst-multiplier: "10"
spec:
  ingressClassName: nginx
  rules:
    - host: tuktuk-dca.web.helium.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tuktuk-dca-service
                port:
                  number: 8123
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: tuktuk-dca-service
  namespace: helium
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: tuktuk-dca-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 75
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75
